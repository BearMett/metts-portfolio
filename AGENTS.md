# AGENTS.md

이 파일은 `/Users/gpc/work/personal/portfolio` 저장소에서 작업하는 Codex용 실행 가이드입니다.

## 1. 목표
- 개인 포트폴리오 웹앱(국문/영문) 유지보수 및 개선
- 변경 시 사용자 노출 품질(번역, UI, 데이터 정확성) 우선

## 2. 실행 환경
- Node.js: `>=20.9.0`
- Package manager: `yarn` (v1)
- Node 18 이하에서는 `next build` 실패

## 3. 자주 쓰는 명령
- 개발: `yarn dev`
- 린트: `yarn lint`
- 타입체크: `./node_modules/.bin/tsc --noEmit`
- 빌드: `yarn build`
- 이력서 PDF 생성: `yarn generate:resume`

참고:
- `yarn build` 전에 `prebuild`로 이력서 PDF 생성이 실행됨

## 4. 현재 구조(핵심만)
- 라우트: `app/`
  - `app/page.tsx`, `app/portfolio/page.tsx`는 서버에서 데이터 준비 후 클라이언트 컴포넌트에 전달
- 클라이언트 페이지 구성: `components/client/`
- 포트폴리오 상호작용 UI: `components/interactive-portfolio.tsx`
- 서버 데이터 조합: `lib/server/`
- 포트폴리오 원천 데이터: `lib/data/portfolio/*.json`
- 회사 메타데이터: `lib/data/companies/*.json`
- 다국어 문자열: `lib/resource.const.ts`
- 로컬라이징 유틸: `lib/portfolio-utils.ts`

## 5. 작업 원칙
- 타입 우선: 임시 `any` 최소화
- 변경 범위 최소화: 요청 범위 밖 리팩터링 지양
- 기존 변경사항 존중: 사용자가 만든 수정은 되돌리지 않음
- UI 텍스트는 기본적으로 번역 가능 상태 유지

## 6. 데이터/번역 동기화 규칙
새 카테고리나 문구 추가 시 아래를 함께 확인:
1. `lib/data/portfolio-categories.json`
2. `lib/resource.const.ts` (`portfolio.categories.*`)
3. `components/interactive-portfolio.tsx` (아이콘/색상 매핑)
4. 관련 `lib/data/portfolio/*.json` 항목

## 7. 알려진 주의사항
- `lib/consts.ts` 값은 클라이언트 사용 시 `NEXT_PUBLIC_*` 여부 점검 필요
- `components/social-links.tsx`는 lint 규칙(`react-hooks/set-state-in-effect`) 점검 필요
- `components/interactive-portfolio.tsx`는 복잡도가 높아 수정 시 회귀 확인 필수

## 8. 커밋 가이드
- 의미 단위로 분리 커밋 (리팩터링/콘텐츠/문서 분리)
- 커밋 메시지 형식: `gitmoji + 국문 요약`
- 예시:
  - `♻️ 리팩터링: 서버 데이터 계층 분리`
  - `📝 콘텐츠: 포트폴리오 문구 수정`
  - `📝 문서: AGENTS 가이드 업데이트`

## 9. 완료 전 체크리스트
- [ ] 요청 범위 외 변경이 없는가?
- [ ] `tsc --noEmit` 통과
- [ ] 관련 lint 통과
- [ ] 번역 키/카테고리 매핑 누락 없음
- [ ] 빌드 영향 변경이면 `yarn build` 또는 최소 대체 검증 수행
